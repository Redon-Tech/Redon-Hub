"""
    File: /bot/cogs/
    Usage: 
"""
from discord.ext.commands import Cog


class Template(Cog):
    def __init__(self, bot):
        self.bot = bot


async def setup(bot):
    await bot.add_cog(Template(bot))
